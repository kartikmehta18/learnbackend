<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <script src="https://cdn.tailwindcss.com"></script>
    <title>Document</title>
  </head>
  <body>
    <div class="main w-full min-h-screen bg-zinc-900">
      
      <div class="form p-10 text-white">
        <h1 class="mb-4 text-3xl">Notes App</h1>
        <form action="/create" method="post">
          <input
            type="text"
            required
            class="block px-5 py-2 bg-zinc-800 w-full rounded-lg text-white outline-none"
            placeholder="Titele goes here"
            name="title"
          />
          <textarea
            required
            class="block px-5 py-2 bg-zinc-800 resize-none mt-4 w-full rounded-lg text-white outline-none"
            placeholder="write your task details"
            name="details"
          ></textarea>
          <input
            type="submit"
            placeholder="creat task"
            class="mt-4 bg-blue-500 text-white rounded-md px-5 py-2 cursor-pointer"
          />
        </form>
      </div>
      <div class="tasks flex gap-3 flex-wrap p-10">
        <% if(files.length >0){ %> <% files.forEach(function(val){%>
        <div class="task min-w-72 px-3 py-4 rounded-md bg-zinc-800">
          <h1 class="text-white text-3xl tracking-tighter"><%= val %></h1>
          <div class="flex w-full justify-between mt-3 item-center">
            <a class="text-blue-500 inline-block " href="/files/<%= val %>"
              >Read more</a>
              <a class="text-zinc-600" href="/edit/<%= val %>">edit filename</a>
              <a class="text-green-600" href="/edittxt/<%= val %>">edit text</a>
          </div>
        </div>

        <% }) %> <% } else{%>

        <h3 class="text-zinc-600">No task yet</h3>

        <% } %>
      </div>
    </div>
  </body>
</html>

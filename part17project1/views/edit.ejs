<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link href="./stylesheets/style.css" rel="stylesheet" />
    <script src="https://cdn.tailwindcss.com"></script>
    <title>Create User</title>
  </head>
  <body class="w-full h-full bg-zinc-900 text-white">
    <div class="w-full h-full bg-zinc-900 text-white p-4">
        <div class="flex justify-end">
            <a class="bg-red-500 rounded-md px-3 py-2 inline-block mb-5" href="/logout">Logout</a>
        </div>
      
      <h3 class="text-3xl text-white mb-5"><span class="font-light">Edit your blog</h3>
      <form method="post" action="/update/<%= post._id %>">
        <textarea  placeholder="What's on your mind ?"  class=" block p-2  outline-none w-1/3 resize-none bg-transparent border-2 border-zinc-800 rounded-md" name="content" id="" required ><%= post.content %></textarea>
        <input class="p bg-transparent border-2 border-zinc-800 rounded-md mt-2" type="text"name="url" placeholder="url" value="<%= post.url %> "  >
        <input class=" text-black text-xl py-2 bg-yellow-500 block rounded-md mt-4 w-40" type="submit" value="Update post">
      </form>

    
    </div>
</html>